{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "@types/node": "^24.1.0"
  },
  "dependencies": {
    "react-icons": "^5.5.0",
    "react-wordcloud": "^1.2.7",
    "recharts": "^3.1.0"
  }
}
